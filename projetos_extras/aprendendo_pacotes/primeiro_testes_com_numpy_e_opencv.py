import numpy as np
import cv2

#As 5 Funções que você vai usar 99% do tempo

#Não aprenda as centenas de funções que ele tem. Foque nestas:

#np.zeros((altura, largura)): Cria uma imagem preta.

#np.ones((altura, largura)): Cria uma imagem branca.

#.shape: Te diz o tamanho da sua imagem (ex: (1080, 1920, 3)).

#array[y1:y2, x1:x2]: O Slicing. É como você "clica e arrasta" para selecionar uma região.

#array.astype(np.uint8): Transforma os números em formato de "pixel" (0 a 255). Imagens exigem isso. 




print(np. zeros((250, 250)))